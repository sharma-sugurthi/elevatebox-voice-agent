[pytest]
asyncio_mode = auto
markers =
    integration: marks tests as requiring real API keys (deselect with '-m "not integration"')
    live: marks tests that make real external API calls (deselect with '-m "not live"')
